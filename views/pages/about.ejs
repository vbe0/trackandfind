<!DOCTYPE html>
<html lang="en">

    <% include ../partials/head %>
    <head>

    </head>
    
    <body>
        <% include ../partials/nav %>
        <!-- content for about page -->
        <div class="container" id="about">
            <div class="row">
                <h2 class="text-center">GG Page</h2>
                    <div class="col-xs-12">
                        <p>Test.</p>
                        <div class="container">
                            <button id="mybutton" onclick="clicking()" type="button" class="btn btn-danger" style="width:500px;height:200px; font-size: 50px;">Click to test App</button>
                        </div>
                        <p id="testapp" style="font-size: 50px;"></p>
                    </div>


                    <script>
                        function clicking()
                        {
                            var elem = document.getElementById("testapp")
                            console.log(elem.value)
                            elem.innerHTML = "Testing..."
                            sleep(1000).then(() => {
                                elem.innerHTML = "Done!"
                            });
                            }
                        function sleep (time) 
                        {
                            return new Promise((resolve) => setTimeout(resolve, time));
                        }
                    </script>

            </div>
        </div><!-- end of content -->
        <% include ../partials/scripts %>

    </body>
</html>