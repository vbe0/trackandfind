doctype html
html(lang="en")
head
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link#bootstrap-css(href="//netdna.bootstrapcdn.com/bootstrap/3.1.0/css/bootstrap.min.css" rel="stylesheet")
    script(src="//netdna.bootstrapcdn.com/bootstrap/3.1.0/js/bootstrap.min.js")
    script(src="public/scripts/thingslist.js")
    
.container
    table#things_table.table.table-bordered(style="table-layout: fixed; width: 100%;")
        colgroup
            col( width='10%')
            col( width='10%')
            col( width='34%')
            col( width='11%')
            col( width='10%')
            col( width='7%')
            col( width='18%')
        thead
        tr
            th ID
            th Name
            th Description
            th#btnAll(class='text-right')
            th Temperature
            th Battery
            th Date
script(type="text/javascript").
    requestAllThings("live")
    var btnAll = document.getElementById('btnAll')//$('#btnAll')
    console.log(btnAll)
    btnAll.appendChild( makeBtn("btnall", "Hide All"))
    btnAll.appendChild( makeBtn("btnall", "Show All"))